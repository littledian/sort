var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "default", ()=>SelectionSort
);
var _baseSort = require("./BaseSort");
var _baseSortDefault = parcelHelpers.interopDefault(_baseSort);
class SelectionSort extends _baseSortDefault.default {
    sort(array) {
        const length = array.length;
        for(let i = 0; i < length; i++){
            let min = i;
            for(let j = i + 1; j < length; j++)if (array[min] > array[j]) min = j;
            this.exchange(array, i, min);
        }
        return array;
    }
}
