var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
parcelHelpers.export(exports, "default", ()=>QuickSort
);
var _baseSort = require("./BaseSort");
var _baseSortDefault = parcelHelpers.interopDefault(_baseSort);
class QuickSort extends _baseSortDefault.default {
    async _partition(array, lo, hi) {
        let i = lo;
        let j = hi + 1;
        let v = array[lo];
        while(true){
            while(array[++i] < v);
            while(array[--j] > v);
            if (i >= j) break;
            this.exchange(array, i, j);
            await this.sleep(1);
        }
        this.exchange(array, j, lo);
        await this.sleep(1);
        return j;
    }
    async _sort(array, lo, hi) {
        if (hi <= lo) return;
        const j = await this._partition(array, lo, hi);
        await this._sort(array, lo, j - 1);
        await this._sort(array, j + 1, hi);
    }
    async sort(array) {
        await this._sort(array, 0, array.length - 1);
        return array;
    }
}
